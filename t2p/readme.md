# Imagine Pro - AI 绘图生成器 (PHP版)

![Project Preview](./2026-02-18%20201535.png)

**Imagine Pro** 是一个轻量级的 AI 生图 Web 页面。基于 PHP 构建，接入了 **讯飞星辰 MaaS** 的文生图 API。它提供了一个极简但功能强大的暗色系界面，允许用户通过调整参数生成高质量的 AI 图像。

## ✨ 主要特性

* **现代化 UI 设计**: 采用 Tailwind CSS 构建的响应式暗黑模式界面，适配桌面端与移动端。
* **多模型支持**: 内置支持 `Qwen-Image-2512` (通义千问) 和 `Z-Image-Turbo` 等模型切换。
* **丰富的参数控制**:
    * **正向/负向提示词**: 支持详细的画面描述与排除词。
    * **画幅比例**: 提供 1:1, 3:4, 4:3, 9:16, 16:9 多种主流比例，可视化选择。
    * **采样器 (Sampler)**: 支持 Euler a, DPM++ 等多种采样算法。
    * **高级设置**: 可调节迭代步数 (Steps)、提示词相关度 (CFG Scale) 及随机种子 (Seed)。
* **无刷新体验**: 使用 AJAX 异步请求，生成过程流畅，无需刷新页面。
* **安全代理**: 后端 PHP 负责 API 鉴权（HMAC-SHA256 签名），保护 API Key 不暴露在前端。


## 🚀 快速开始

### 1. 获取 API 密钥
本项目依赖讯飞开放平台的文生图服务。
1. 访问 [讯飞星辰 MaaS 控制台](https://maas.xfyun.cn/modelService)。
2. 创建应用并开通 **文生图** 相关模型服务。
3. 获取 `APPID`, `APIKey`, `APISecret`。

![MaaS Platform](./2026-02-18%20201828.png)
> *图：讯飞星辰 MaaS 模型广场，您可以在此查看可用模型*

### 2. 安装与配置

将项目代码下载到您的 Web 根目录，并编辑 `config.php` 文件：

```php
// config.php
return [
    // 替换为您在讯飞开放平台获取的密钥
    'APP_ID' => '您的APPID',
    'API_KEY' => '您的APIKey',
    'API_SECRET' => '您的APISecret',

    // API 接口地址 (通常无需修改，除非使用不同区域)
    'API_URL' => '[https://maas-api.cn-huabei-1.xf-yun.com/v2.1/tti](https://maas-api.cn-huabei-1.xf-yun.com/v2.1/tti)',
    
    // 可在数组中添加更多支持的模型
    'MODELS' => [
        'xopqwentti20b' => 'Qwen-Image-2512 (旗舰画质)',
        'xopzimageturbo' => 'Z-Image-Turbo (极速生成)'
    ]
];
